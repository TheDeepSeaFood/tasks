// Fill after deploying the Apps Script web app (see the foundation plan).
window.APP_CONFIG = {
  CLIENT_ID: 'REPLACE_WITH_CLIENT_ID',  // same Google OAuth Web client ID as Config.gs
  API_URL:   'REPLACE_WITH_API_URL'     // Apps Script web app /exec URL
};
