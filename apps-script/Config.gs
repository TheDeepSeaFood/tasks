// Fill these three values after creating the OAuth client + backend spreadsheet.
// See docs/superpowers/plans/2026-07-10-task-mgmt-foundation.md  (Prerequisites P1, P2)

const CLIENT_ID       = 'REPLACE_WITH_CLIENT_ID';         // Google OAuth Web client ID
const ALLOWED_DOMAIN  = 'REPLACE_WITH_DOMAIN';            // Workspace domain, e.g. 'thedeepseafood.com' — set '' to allow any
const SPREADSHEET_ID  = 'REPLACE_WITH_SPREADSHEET_ID';    // backend spreadsheet ID from its URL
